/** Landing demo akışı için statik örnek görseller (public/samples) */

const base = "/samples";

export const SAMPLE_IMAGES = {
  demoGarment: `${base}/demo-garment.jpg`,
  uploadThumb: `${base}/upload-thumb.jpg`,
} as const;
